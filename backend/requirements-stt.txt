# WhisperX (STT + 강제정렬 + 화자분리) 전용 의존성.
#
#   pip install -r requirements-stt.txt
#
# ⚠️ torch 는 환경마다 설치법이 다릅니다.
#   - NVIDIA GPU : https://pytorch.org 에서 CUDA 버전에 맞는 명령을 먼저 실행
#   - Apple Silicon / CPU : 아래 그대로 설치하면 됩니다 (느립니다)
#
# ⚠️ 화자분리(pyannote) 는 HuggingFace 토큰과 모델 약관 동의가 필요합니다.
#   1) https://huggingface.co/settings/tokens 에서 read 토큰 발급
#   2) pyannote/speaker-diarization-3.1 과 pyannote/segmentation-3.0 페이지에서
#      "Agree and access repository" 클릭
#   3) .env 의 HF_TOKEN 에 토큰을 넣기

torch>=2.1.0
torchaudio>=2.1.0
whisperx>=3.3.0
faster-whisper>=1.0.0
pyannote.audio>=3.1.0
